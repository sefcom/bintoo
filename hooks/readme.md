```
# n is the optimization level (0, 1, 2 or 3)
export VARNAMES\_OPT=-On
```

```
export VARNAMES\_gcc=gcc\_path (execve complains when it can't find cc1)
export VARNAMES\_clang=clang\_path (no complains yet!)
```
