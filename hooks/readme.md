```
# n is the optimization level (0, 1, 2 or 3)
export VARNAMES_OPT=-On
```

```
export VARNAMES_gcc=gcc_path (execve complains when it can't find cc1)
export VARNAMES_clang=clang_path (no complains yet!)
```
